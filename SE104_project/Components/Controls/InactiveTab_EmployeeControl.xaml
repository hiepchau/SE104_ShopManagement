<UserControl x:Class="SE104_OnlineShopManagement.Components.Controls.InactiveTab_EmployeeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SE104_OnlineShopManagement.Components.Controls" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="45" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="35*"/>
        </Grid.ColumnDefinitions>
        <TextBlock 
            x:Name="txtID"
            Grid.Column="0"
            Text="{Binding LastName}"      
            HorizontalAlignment="Left"
            Style="{StaticResource Text}"
            Margin="10,0,0,0" />
        <TextBlock          
            x:Name="txtEmail"
            Grid.Column="1" 
            HorizontalAlignment="Left"                               
            Text="{Binding Email}"
            TextTrimming="CharacterEllipsis"
            Style="{StaticResource Text}" 
            Margin="10,0,0,0" />
        <TextBlock      
            x:Name="txtRole"
            Grid.Column="2" 
            Text="{Binding role}" 
            Style="{StaticResource Text}" 
            Margin="20,0,0,0" />
    </Grid>
</UserControl>